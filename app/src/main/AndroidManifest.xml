<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ── Разрешения ─────────────────────────────────────────── -->

    <!-- Интернет для Firebase Firestore -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Точные будильники (Android 12+ / API 31+)
         Пользователь должен вручную разрешить через Settings → Apps → Special app access
         Запрашиваем через AlarmManager.canScheduleExactAlarms() перед использованием -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <!-- Fallback для Android 13+ (USE_EXACT_ALARM не требует ручного разрешения,
         но доступен только для определённых категорий приложений) -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <!-- Push-уведомления (Android 13+ / API 33+ требует явного разрешения) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Запуск после перезагрузки (для восстановления AlarmManager) -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- WorkManager требует для запуска фоновых задач -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:name=".TodoApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TodoApp">

        <!-- Splash Screen (Android 12+) -->
        <meta-data
            android:name="androidx.core.splashscreen.SPLASH_SCREEN_ICON"
            android:resource="@mipmap/ic_launcher" />

        <!-- ── MainActivity ─────────────────────────────────── -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.TodoApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ── WorkManager инициализация через Hilt ─────────── -->
        <!-- Отключаем стандартный провайдер, чтобы Hilt мог инжектить в Workers -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <!-- ── BroadcastReceiver для восстановления будильников после перезагрузки -->
        <receiver
            android:name=".worker.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- ── BroadcastReceiver для срабатывания AlarmManager ── -->
        <receiver
            android:name=".worker.AlarmReceiver"
            android:exported="false" />

    </application>

</manifest>
